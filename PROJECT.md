# ASA Fan Website

Static marketing + product-catalog site for **ASA Fan** (Adda South America Corporation), a Brazilian manufacturer of microventiladores AC/DC, ventiladores axiais, and acessórios (telas, grelhas, porta-filtros, termostatos). Founded 1999, based in Cotia/SP.

- **Live:** https://eulahwu917.github.io/asafan-website/
- **Hosting:** GitHub Pages (auto-deploys from `main`)
- **Stack:** Hand-written HTML / CSS / vanilla JS — no build step, no framework, no package.json
- **Fonts:** Inter via Google Fonts. **Icons:** Lucide (CDN). **Color tokens:** navy `#1A1F60`, red `#CC0000`.

## Pages

| File | Purpose |
|---|---|
| [index.html](index.html) | Homepage — hero video, portfolio cards, value props, 3-step process, CTA |
| [quem-somos.html](quem-somos.html) | About — timeline + quote + stats |
| [produtos.html](produtos.html) | **Product catalog** — 77 cards, two-tier tipo filter + contextual secondary filters |
| [microventiladores.html](microventiladores.html) | Category landing (explainer/SEO) — *still has placeholder content* |
| [axiais.html](axiais.html) | Category landing — *still has placeholder content* |
| [acessorios.html](acessorios.html) | Category landing — *still has placeholder content* |
| [aplicacoes.html](aplicacoes.html) | 6 application cards (compressores, painéis elétricos, elevadores, automação, peltier, equipamentos médicos) |
| [carreira.html](carreira.html) | Career — value props + application form (mailto:) |
| [fale-conosco.html](fale-conosco.html) | Contact — form (mailto:) + address + map embed |

All pages share the same header (with Produtos dropdown) and 4-column footer. Every CTA links to `https://wa.me/551134065088` with a pre-filled message.

## Project structure

```
/
├── *.html                         # 9 pages (above)
├── css/style.css                  # all styles (one file)
├── js/main.js                     # menu toggle, catalog filters, contact form handler
├── assets/
│   ├── logo/                      # favicon, hero-bg, logos
│   ├── partner-logos/             # 6 partner PNGs
│   ├── product/                   # ~46 individual product photos (lowercase código.jpg)
│   ├── product-description/       # spec sheets, thumbnails
│   ├── video/hero-video.mp4       # ~9.6 MB — flagged for compression
│   └── website-ui/                # cta-bg, etc.
├── scripts/
│   └── generate_cards.py          # rebuilds 77 product cards from the xlsx
├── sessions/                      # gitignored — per-session work logs (orientation source)
├── raw_assets/                    # gitignored — source photos, spreadsheets, customer drops
├── project files/                 # gitignored — customer-supplied originals (xlsx, zips)
├── REFERENCE.md                   # original Portuguese copy + page contents reference
├── sitemap.xml, robots.txt
```

## Product catalog data model

Source of truth: **`raw_assets/product_site/dados para site.xlsx`** (sheet `Planilha1`, ~89 rows). Cards are generated by [scripts/generate_cards.py](scripts/generate_cards.py) and pasted into [produtos.html](produtos.html).

77 products grouped by SKU prefix:

| Prefix | Tipo | Count |
|---|---|---|
| `A`, `E` | Microventilador AC (110V/220V/bivolt) | 10 |
| `D` | Microventilador DC (12V/24V/48V) | 30 |
| `X` | Axial (soprador/exaustor, 220V mono / 220V-380V tri) | 21 |
| `M` | Telas/máscaras metálicas | 7 |
| `G` | Grelhas ABS | 3 |
| `P` | Porta-filtros | 3 |
| `T` | Termostatos | 3 |

**Filter conventions** (data-attributes on `<article class="product-card">`):
- `data-tipo` — `micro-ac` | `micro-dc` | `axial` | `acessorio`
- `data-subtipo` — `tela` | `grelha` | `porta-filtro` | `termostato` (only on acessórios)
- `data-tensao` — `12v` | `24v` | `48v` | `110v` | `220v` | `bivolt` | `220v-380v`
- `data-mancal` — `rolamento` | `bucha` (microventiladores)
- `data-funcao` — `soprador` | `exaustor` (axiais)

**Contextual filter visibility** — `.filter-group[data-context="..."]` lists which `tipo` values reveal that group; the JS in [js/main.js](js/main.js) hides non-matching groups and ignores their values when filtering. Two separate Tensão groups exist (DC vs Axial); active state is scoped to the parent `.filter-group` to keep them independent.

**Image lookup** — each card's `<img src>` points to `assets/product/<código-lowercase>.jpg|png`; missing photos fall back to `assets/product/placeholder.png` ("Foto em breve").

## Conventions

- **Company name:** "ASA Fan" everywhere (not ASAFAN, AsaFan, etc.).
- **Image filenames:** lowercase código (`a81.jpg`, `d91-2.jpg`) — required because GitHub Pages is case-sensitive.
- **No build pipeline** — edit HTML/CSS/JS directly; commit; push to `main` → Pages deploys.
- **Forms:** all use `mailto:` (static-host limitation). Replacement is on the deferred list.
- **External links:** `target="_blank" rel="noopener noreferrer"`.
- **Accessibility:** form `<label for>`/`<input id>` paired everywhere; nav has `aria-label`; dropdown supports `:focus-within`.
- **Structured data:** Organization JSON-LD on every page; BreadcrumbList where applicable. `sameAs` is empty (TODO).
- **Telemetry/analytics:** none configured.

## Customer-supplied source data

- **`raw_assets/product_site/dados para site.xlsx`** — canonical product spreadsheet (do not infer specs from the rendered HTML; regenerate via `generate_cards.py`).
- **`raw_assets/product_site/FOTOS ATUAIS_/`** — original studio photos (38 files, named by código).
- **`raw_assets/product_site_update/`** — 2026-05-04 photo drop. Subfolders: `AC/ DC/ AXIAIS/ ACESSÓRIOS/ FOTOS ATUAIS_/`. Routing convention used by [scripts/integrate_photos.py](scripts/integrate_photos.py): `FOTOS ATUAIS_/` is canonical for AC/DC solo photos; `AXIAIS/` and `ACESSÓRIOS/` are the only sources for those categories. Files referencing only manufacturer model numbers (not in xlsx CÓDIGO) are ignored.

## Outstanding work

Carried across sessions, priority-ordered:

1. **Photographer punch-list (16 códigos still on placeholder)** — see [sessions/2026-05-04-photo-update.md](sessions/2026-05-04-photo-update.md) for the table. E122 + 12 axiais (X201-X454 minus those covered by group shots) + M80, M90, M200.
2. **New SKUs in 2026-05-04 drop not yet in xlsx** — 3 porta-filtros (P40/P50/P60), ~13 telas (3xxxP series), ~10 axiais (4xxxA/5xxxA series). Customer to confirm which are catalog additions, then append rows to `dados para site.xlsx` and re-run integration scripts.
3. **Category landing pages** ([microventiladores.html](microventiladores.html), [axiais.html](axiais.html), [acessorios.html](acessorios.html)) still have last-redesign placeholder content.
4. **URL-driven filters** on `produtos.html` (`?tipo=x&tensao=y`) for deep-linking from category pages.
5. **Image consistency** — product photos have varied environmental backgrounds; standardize to white-bg cutouts or compress.
6. **Forms** — replace `mailto:` with Formspree or a tiny PHP/Functions endpoint.
7. **Hero video compression** (`assets/video/hero-video.mp4` ≈ 9.6 MB).
8. **Empty `sameAs`** in Organization JSON-LD — populate when social URLs exist.
9. **Per-SKU `object-position`** — currently inline on E171 (managed via `IMG_STYLE_OVERRIDES` in `generate_cards.py`). If more photos need cropping tweaks, add them to that dict.

## Where to look

- **Per-session work logs:** [sessions/](sessions/) — each session has a dated `.md` covering objective, decisions, files touched, and outstanding items. Read these to get oriented before resuming work. (Folder is gitignored, so logs are only on the local machine.)
- **Original page copy / company info:** [REFERENCE.md](REFERENCE.md) — Portuguese source-of-truth for copy.
